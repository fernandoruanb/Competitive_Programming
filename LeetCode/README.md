<p align="center">
  <img src="https://assets.leetcode.com/static_assets/public/images/LeetCode_Sharing.png" alt="LeetCode Cover" width="700"/>
</p>

<h1 align="center">LeetCode Roadmap</h1>


This roadmap tracks the exercises I am solving, the main topic behind each problem, and the key lesson learned from each one.

| # | Problem | Type | Main Learning | My Solution | Discussion link |
|---|---------|------|---------------|---------------|----------------|
| 704 | [Binary Search](https://leetcode.com/problems/binary-search/) | Binary Search / Array | Binary search is useful for searching inside a sorted array. The middle element should be calculated with `mid = start + (end - start) / 2` to avoid overflow and keep the search range correct. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/704/704.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/704/) |
| 268 | [Missing Number](https://leetcode.com/problems/missing-number/) | Array / Math | Not every problem should be forced into binary search. For this problem, the mathematical sum formula is more direct: the expected sum from `0` to `n` is `n * (n + 1) / 2`. The missing number is the difference between the expected sum and the actual sum of the array. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/268/268.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/268/) |
| 35 | [Search Insert Position](https://leetcode.com/problems/search-insert-position/) | Binary Search / Array | Binary search can also find where a number should be inserted. When the target does not exist, the final `start` or `left` index points to the exact position where the value should be placed. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/35/35.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/35/) |
| 74 | [Search a 2D Matrix](https://leetcode.com/problems/search-a-2d-matrix/) | Matrix / Binary Search | This problem applies the same search idea to a matrix. The goal is direct: analyze the matrix indexes and return `true` if the target exists, or `false` if it does not. It reinforces that a matrix can also be treated as a searchable structure. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/74/74.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/74/) |
| 1351 | [Count Negative Numbers in a Sorted Matrix](https://leetcode.com/problems/count-negative-numbers-in-a-sorted-matrix/) | Matrix / Array Traversal | This problem introduces matrix traversal using indexes. The main idea is to iterate through the rows and columns carefully, analyzing each position of the matrix to count the negative numbers. Since the matrix is sorted row-wise and column-wise, the traversal can later be optimized. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/1351/1351.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/1351/) |
| 981 | [Time Based Key-Value Store](https://leetcode.com/problems/time-based-key-value-store/) | Hash Map / Binary Search / Design | I learned how to work with C++ containers such as `map`, `vector`, and `pair`, storing multiple values for the same key and using binary search to find the closest timestamp. If the exact timestamp is not found, the binary search keeps track of the previous valid value smaller than the target timestamp. If such value exists, it is returned; otherwise, an empty string is returned. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/981/981.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/981/) |
| 1 | [Two Sum](https://leetcode.com/problems/two-sum/) | Array / Two Pointers / Sorting | I learned that when the vector is unordered, I can reorganize the values and use a two pointers approach to navigate from the beginning and the end. If the sum is too high, I decrease `right`; if the sum is too low, I increase `left`. I also learned that when I need to preserve the original indexes after sorting, I can use a `vector<pair<int, int>>`, storing each value together with its original index. This allows me to sort the values, search efficiently, and still return the correct original indexes. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/1/1.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/1/) |
| 15 | [3Sum](https://leetcode.com/problems/3sum/) | Array / Two Pointers / Sorting | I learned that a brute force solution with three nested loops gives `O(n³)` and can fail because of time limit. To optimize it, I first sort the vector, then iterate with `i` while avoiding duplicate values. For each `i`, I use two pointers: `left = i + 1` and `right = nums.size() - 1`. Everything depends on the sum: if the sum is equal to zero, I store the triplet in the result vector and move both pointers while skipping duplicates. If `sum > 0`, I decrease `right` to make the sum smaller. If `sum < 0`, I increase `left` to make the sum bigger. With this approach, I can avoid brute force and return the correct vector of triplets. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/15/15.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/15/) |
| 169 | [Majority Element](https://leetcode.com/problems/majority-element/) | Array / Boyer-Moore Voting / Sorting | I learned that sorting the vector works because the majority element always appears more than `n / 2` times, so after sorting it must be located at the middle position. However, sorting gives `O(n log n)`. To make the solution faster, I learned the Boyer-Moore Voting Algorithm, which uses one candidate and one counter. When the counter is zero, I choose a new candidate. If the current number is equal to the candidate, I increase the counter. If it is different, I decrease the counter. Since the problem guarantees that a majority element exists, the final candidate is the answer. This improves the solution to `O(n)` time and `O(1)` memory. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/169/169.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/169/) |
| 414 | [Third Maximum Number](https://leetcode.com/problems/third-maximum-number/) | Array / Sorting / Distinct Values | I learned that sorting the vector makes it easier to search from the greatest value to the smallest one. The main challenge is that the third maximum must be a distinct number, so duplicated values cannot increase the counter. To solve this, I start from the right side of the sorted vector and keep a `maximum` variable initialized with a value greater than `INT_MAX` using `long long int`. Whenever I find a number smaller than the previous maximum, I update `maximum` and increase the counter. If the counter reaches `3`, I return the third distinct maximum. If there are fewer than three distinct values, I return the greatest number. This solution avoids counting duplicates and handles edge cases like `INT_MIN`. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/414/414.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/414/) |
| 349 | [Intersection of Two Arrays](https://leetcode.com/problems/intersection-of-two-arrays/) | Array / Sorting / Two Pointers | I learned that sorting both vectors allows me to use a two pointers approach to find common values efficiently. After sorting `nums1` and `nums2`, I use `i` to walk through `nums1` and `j` to walk through `nums2`. If both values are equal, I found an intersection value. Since the result must contain only distinct values, I only push the number if the result vector is empty or if the last inserted value is different from the current one. If `nums1[i]` is smaller, I move `i` forward. If `nums2[j]` is smaller, I move `j` forward. This avoids brute force comparison and keeps the logic clean. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/349/349.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/349/) |
| 217 | [Contains Duplicate](https://leetcode.com/problems/contains-duplicate/) | Array / Sorting / Duplicate Detection | I learned that sorting the vector makes duplicate detection much easier because equal values become neighbors. After sorting `nums`, I keep a `candidate` initialized with the first element and start checking from index `1`. If the current number is equal to the candidate, it means a duplicate exists and I can immediately return `true`. Otherwise, I update the candidate with the current value and continue scanning the vector. If the loop finishes without finding equal adjacent values, then the array has no duplicates and I return `false`. This avoids comparing every element with every other element and reduces the brute force idea from `O(n²)` to `O(n log n)` because of sorting. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/217/217.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/217/) |
| 283 | [Move Zeroes](https://leetcode.com/problems/move-zeroes/) | Array / Two Pointers / In-place Modification | I learned that using `erase()` and `push_back()` can make the solution slower because removing elements from the middle of a vector forces other elements to shift. A better approach is to use two pointers: `read` to scan the array and `write` to place the next non-zero value. First, I copy every non-zero number to the front of the vector while preserving their original order. After all non-zero values are written, I fill the remaining positions with zeroes. This solves the problem in-place, keeps the relative order of non-zero elements, and improves the solution to `O(n)` time. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/283/283.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/283/) |
| 121 | [Best Time to Buy and Sell Stock](https://leetcode.com/problems/best-time-to-buy-and-sell-stock/) | Array / Greedy / One Pass | I learned that I do not need to test every possible buy and sell combination. Instead, I can scan the prices only once while keeping the smallest price seen so far as the best buying opportunity. For each price, if it is smaller than my current `buy`, I update `buy`. Otherwise, I calculate the possible profit using the current price as the selling price. If this profit is greater than the best profit found so far, I update `profit`. This approach works because the buy day always comes before the sell day while I move from left to right. It avoids brute force `O(n²)` and solves the problem in `O(n)` time with `O(1)` memory. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/121/121.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/121/) |
| 509 | [Fibonacci Number](https://leetcode.com/problems/fibonacci-number/) | Recursion / Math / Dynamic Programming | I learned that the Fibonacci sequence can be solved directly from its recursive definition: `fib(n) = fib(n - 1) + fib(n - 2)`. The base cases are important because they stop the recursion: if `n == 0`, I return `0`, and if `n == 1`, I return `1`. After that, each call breaks the problem into two smaller Fibonacci calls. This helped me understand how recursion builds the answer from smaller subproblems. However, this simple recursive approach recalculates the same values many times, so its time complexity is exponential. A faster version would use dynamic programming or an iterative approach to avoid repeated work. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/509/509.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/509/) |
| 118 | [Pascal's Triangle](https://leetcode.com/problems/pascals-triangle/) | Array / Dynamic Programming / Matrix Construction | I learned that Pascal's Triangle can be built row by row using the previous row as reference. The first row is always `[1]`, and the second row is `[1, 1]`. For each new row, I start by adding `1`, then each middle value is calculated by summing two adjacent values from the previous row: `prev[counter - 1] + prev[counter]`. After filling the middle values, I add another `1` at the end. This showed me how dynamic programming works in a simple way: each row depends on the row before it. The solution builds the triangle progressively until it reaches `numRows`. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/118/118.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/118/) |
| 9 | [Palindrome Number](https://leetcode.com/problems/palindrome-number/) | Math / Integer Reversal | I learned that a number is a palindrome if it remains the same when its digits are reversed. First, I handle the negative case because negative numbers cannot be palindromes due to the `-` sign. Then I create a copy of the original number and build a reversed version digit by digit. In each iteration, I multiply the reversed number by `10`, get the last digit using `% 10`, add it to the reversed number, and remove the last digit from the copy using `/ 10`. At the end, if the reversed number is equal to the original number, the integer is a palindrome. I used `long long int` to make the reversed value safer against overflow during the calculation. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/9/9.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/9/) |
| 13 | [Roman to Integer](https://leetcode.com/problems/roman-to-integer/) | Hash Map / String / Parsing | I learned how to convert Roman numerals by mapping each Roman character to its integer value using an `unordered_map`. The main idea is to compare the current symbol with the next one. If the current value is smaller than the next value, it means I found a subtractive case, such as `IV`, `IX`, `XL`, `XC`, `CD`, or `CM`, so I add `next - current` to the result and move the index by two positions. Otherwise, I simply add the current value and move normally. This problem helped me understand how local comparisons inside a string can be used to correctly interpret special number rules. | [Solution](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/13/13.cpp) | [Discussion](https://github.com/fernandoruanb/Competitive_Programming/tree/main/LeetCode/official/13/) |
